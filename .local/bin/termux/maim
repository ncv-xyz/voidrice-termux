#!/bin/sh

# maim wrapper for Termux

if [ -n "$SSH_CLIENT" ] || [ -n "$SSH_TTY" ]; then
	for var in "$@" ; do case "$var" in
		-*) ;;
		*) args="$args $var" ;;
	esac done
	set -- "${args# }"
	sudo screencap "$@"
else
	"$PREFIX"/bin/maim $@
fi
