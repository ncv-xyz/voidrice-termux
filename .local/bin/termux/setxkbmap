#!/bin/sh

if [ -n "${TMUX}" ]; then
	case "${1}" in
		-q*)
			lang="${LANG#*_}"
			lang="${lang%.*}"
			lang="$(printf %s "${lang}" | tr '[:upper:]' '[:lower:]')"
			printf %s\\n "layout:      ${lang}"
			;;
	esac
else
	"${PREFIX}/bin/setxkbmap" "${@}"
fi
