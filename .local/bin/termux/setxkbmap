#!/bin/sh

if [ -n "$SSH_CLIENT" ] || [ -n "$SSH_TTY" ]; then
	case "$1" in
		-q*)
			lang="${LANG#*_}"
			lang="${lang%.*}"
			lang="$(echo "$lang" | tr "[A-Z]" "[a-z]")"
			echo "layout:      $lang"
			;;
	esac
else
	"$PREFIX"/bin/setxkbmap "$@"
fi
