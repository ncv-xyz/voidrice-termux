#!/bin/sh

if [ -n "$TMUX" ]; then
	case "$1" in
		-q*)
			lang="${LANG#*_}"
			lang="${lang%.*}"
			lang="$(echo "$lang" | tr "[A-Z]" "[a-z]")"
			printf "%s\n" "layout:      $lang"
			;;
	esac
else
	"$PREFIX/bin/setxkbmap" "$@"
fi
