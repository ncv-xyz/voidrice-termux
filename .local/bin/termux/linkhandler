#!/bin/sh

# linkhandler wrapper for Termux

if [ -n "${TMUX}" ]; then
	url="${1:-$(termux-clipboard-get)}"
	[ -f "${url}" ] && command="termux-share -d"
	${command:-termux-open-url} "${url}" >/dev/null 2>&1
else
	"${HOME}/.local/bin/linkhandler" "${@}"
fi
