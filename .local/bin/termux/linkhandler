#!/bin/sh

# linkhandler wrapper for Termux

if [ -n "${TMUX}" ]; then
	cmd='termux-open-url'
	url="${1:-$(termux-clipboard-get)}"
	[ -f "${url}" ] && cmd='termux-share -d'
	${cmd} "${url}" >/dev/null 2>&1
else
	"${HOME}/.local/bin/linkhandler" "${@}"
fi
